import 'package:flutter/material.dart';

class MobileScreenReadMeter extends StatefulWidget {
  const MobileScreenReadMeter({super.key});

  @override
  State<MobileScreenReadMeter> createState() => _MobileScreenReadMeterState();
}

class _MobileScreenReadMeterState extends State<MobileScreenReadMeter> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("MobileScreenReadMeter")),);
  }
}